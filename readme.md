# Personal Portfolio Website (v2)

This is version 2 of my portfolio website.

This version demonstrates:

- Semantic HTML5
- External CSS
- Flexbox layout
- CSS Grid
- Responsive design
- Organized file structure
- Git branching workflow

## Technologies Used

- HTML5
- CSS3
- Flexbox
- CSS Grid
- Google Fonts

## Features

- Flexbox navigation bar
- Centered hero section
- Responsive grid layout
- Clean section spacing
- Structured single-page design

## New Updates / Features (v3)

### 1. Section Interactivity

- **About, Projects, and Contact** sections now toggle open/close when navigation buttons are clicked.
- **“View My Work” button** also toggles the Projects section.
- Sections now **appear below the hero section**, so content is visible immediately without scrolling.

### 2. Project Card Interaction

- Cards highlight and lift when clicked (instead of flipping backwards) using:

### 3. Contact Button Hover Effect

- Hovering over the **Show Contact** button changes text from "Show Contact" → "Click to Email Me!"

### 4. Hero Section Adjustments

- padding adjustments for clean spacing.

### 5. JavaScript / jQuery Updates

- Used **jQuery `.click()`, `.slideToggle()`, `.toggleClass()`** for interactivity.
- Removed mixed plain JS that caused errors.
- Wrapped all JS inside `$(document).ready()` for safe execution.
- Ensured **file structure and script order** allow everything to load properly.

## Author

Kosmas Kasolas  
Aspiring Full-Stack Developer
